# Komari-Theme-LuminaPlus v1.2.9

## 三网模式：多套线路

- 三网模式升级为支持配置**两套三网线路**，每套可独立选择 3 个 Ping 任务，并各自指定适用的节点。
- 节点归属互斥：一个节点只使用一套线路（第 1 套优先）；某套未选择任何节点时，自动覆盖其余全部节点（兜底）。
- 未归属任何线路组的节点继续使用原单线路绑定；迷你卡片与列表视图不受影响。
- 旧版单套三网配置自动迁移为「第一套 + 全部节点」，升级后行为不变。
- 校验规则：开启三网模式时，至少一套配满 3 条线路，且任何已开始配置的套都必须恰好 3 条。
