# Komari-Theme-LuminaPlus v1.3.1

## 1. 三网探测点配置调整（上版本难用兄弟们也不说，嘴这么严）

- 单服务器配置面板现在可从全部 Ping 任务中选择。
- 未绑定任务会标注“后台未绑定”并显示告警。
- 开启模拟延迟后，面板会提示该线路将使用模拟数据；关闭时提示不会生成延迟。
- 三网模式支持对未绑定线路显示模拟延迟。

## 2. 新增未登录时隐藏后台入口

- 默认关闭，仅隐藏访客看到的“后台登录”入口。
- 注意：请在确认本功能作用后再开启。
